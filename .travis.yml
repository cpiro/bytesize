language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.5-dev"
  - "nightly"
env:
  - WITH_PINT=true
  - WITH_PINT=false
install:
  - "python setup.py install"
  - "[[ \"$WITH_PINT\" = true ]] && pip install pint || true"
script: nosetests
